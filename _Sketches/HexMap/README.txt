** HexMap **
The start of a hex map drawing / interaction model. Contains a lot of hex map math, and demonstrates low cost updating of tiles.

ex.
var map:HexMap = new HexMap(tileData, cols, rows, tileSize);