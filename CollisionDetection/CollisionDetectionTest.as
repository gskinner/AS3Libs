package  {		import com.gskinner.sprites.CollisionDetection;	import flash.display.Bitmap;	import flash.geom.Rectangle;	import flash.display.Shape;	import flash.display.Sprite;	import flash.events.MouseEvent;		public class CollisionDetectionTest extends Sprite {			// Protected Properties:		protected var canvas:Shape;		protected var previewBmp:Bitmap;			// Initialization:		public function CollisionDetectionTest() {			stage.addEventListener(MouseEvent.MOUSE_MOVE,handleMove);			canvas = new Shape();			addChild(canvas);			previewBmp = new Bitmap(CollisionDetection.bitmapData);			previewBmp.x = previewBmp.y = 10;			addChild(previewBmp);		}			// Protected Methods:		protected function handleMove(evt:MouseEvent):void {			ball.x = stage.mouseX;			ball.y = stage.mouseY;						canvas.graphics.clear();			var intersection:Rectangle = CollisionDetection.check(ball,outer.thing);			previewBmp.visible = (intersection != null);						if (intersection == null) { return; }			canvas.graphics.lineStyle(1,0xFF0000,1);			canvas.graphics.drawRect(intersection.x,intersection.y,intersection.width,intersection.height);		}			}	} 