package {			import com.gskinner.motion.Wander;		import flash.display.Sprite;		[SWF(frameRate="30", width="550", height="550", backgroundColor="#000000")]	public class WanderTest extends Sprite {				public function WanderTest() {			stage.scaleMode = "noScale";			stage.align = "tl";			init();		}				protected function init():void {			var w:Number = stage.stageWidth;			var h:Number = stage.stageHeight;			var canvas:Sprite = new Sprite();			addChild(canvas);						for (var i:uint=0; i<500; i++) {								var dude:Sprite = new Sprite();				dude.graphics.beginFill(0xFFFFFF,1);				dude.graphics.moveTo(-3,-1);				dude.graphics.lineTo(-3,1);				dude.graphics.lineTo(3,0);								dude.rotation = (Math.random()*360/180|0)*180;				dude.x = w*0.5;				dude.y = h*0.5;								new Wander(dude,{speed:Math.random()*3+2,strength:0.02,outerRadius:350,innerRadius:225,targetRotation:-90,targetY:h*0.5,targetX:w*0.5,varyRotation:Math.random()*0.1});								canvas.addChild(dude);			}		}			}}