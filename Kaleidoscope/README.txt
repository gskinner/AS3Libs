** Kaleidoscope **
Create a kaledoscope effect using any display object as the source.

KaleidoscopeTest will work as an ActionScript project without modifications.

ex.
var foo:Kaleidoscope = new Kaleidoscope(src, radius, slices, true);