package {		import com.gskinner.utils.Rndm;	import flash.display.Sprite;	import flash.events.Event;	import flash.events.MouseEvent;		public class RndmTest extends Sprite {			// Constants:			// Public Properties:			// Protected Properties:		protected var count:uint=0;			// Initialization:		public function RndmTest() {			Rndm.seed = 10;			reset();		}			// Protected Methods:		protected function reset(evt:Event=null):void {			Rndm.reset();			addEventListener(Event.ENTER_FRAME,tick);			stage.addEventListener(MouseEvent.CLICK,reset);			graphics.clear();			graphics.moveTo(Rndm.integer(550),Rndm.integer(400));			count = 0;			Rndm.seed = Math.random()*0xFFFFFF;		}				protected function tick(evt:Event):void {			graphics.lineStyle(Rndm.integer(1,5),Rndm.integer(0xFFFFFF),Rndm.float(0.2,1));			graphics.lineTo(Rndm.integer(550),Rndm.integer(400));			if (++count == 50) { removeEventListener(Event.ENTER_FRAME,tick); }		}			}	}